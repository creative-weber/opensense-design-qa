{
  "name": "@opendesign-qa/compare",
  "version": "0.0.0",
  "private": true,
  "description": "Pixel-level visual diff engine for OpenDesign QA",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pixelmatch": "^6.0.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@opendesign-qa/config": "workspace:*",
    "@types/node": "^22.10.2",
    "@types/pixelmatch": "^5.2.6",
    "@vitest/coverage-v8": "^2.1.8",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
